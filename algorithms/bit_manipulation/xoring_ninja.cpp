#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int T;
    cin >> T;
    while (T--) {
        int N;
        cin >> N;
        vector<int> arr;
        while (N--) {
            int temp;
            cin >> temp;
            arr.push_back(temp);
        }
        for (int ii = 1; ii <= n; ii++) {
            
        }
    }
    return 0;
}
